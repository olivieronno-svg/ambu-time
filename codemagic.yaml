workflows:
  ios-workflow:
    name: iOS Workflow
    max_build_duration: 60
    integrations:
      app_store_connect: Codemagic
    environment:
      vars:
        BUNDLE_ID: "com.onnoff.ambutime"
        APP_STORE_APPLE_ID: 6763624759
      flutter: stable
      xcode: latest
      cocoapods: default
    scripts:
      - name: Initialiser la keychain
        script: keychain initialize
      - name: Creer le certificat de distribution iOS (si absent)
        script: |
          # Tente de creer le certificat — si deja existant, erreur ignoree
          app-store-connect certificates create --type IOS_DISTRIBUTION --save 2>&1 || echo "OK - Certificat peut-etre deja existant"
          # Liste et sauvegarde tous les certificats distribution en local
          app-store-connect certificates list --type IOS_DISTRIBUTION --save
      - name: Ajouter les certificats a la keychain
        script: keychain add-certificates
      - name: Creer/recuperer le provisioning profile
        script: |
          app-store-connect fetch-signing-files "$BUNDLE_ID" \
            --type IOS_APP_STORE \
            --create \
            --save
      - name: Appliquer les profils au projet Xcode
        script: xcode-project use-profiles
      - name: Get Flutter packages
        script: flutter pub get
      - name: Install pods
        script: |
          find . -name "Podfile" -execdir pod install \;
      - name: Build ipa
        script: |
          flutter build ipa --release \
            --export-options-plist=/Users/builder/export_options.plist
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
      - flutter_drive.log
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
        # beta_groups retiré pour le premier build — Codemagic uploadera sur
        # TestFlight et l'app sera dispo pour les testeurs internes (App Manager).
        # Ajouter un groupe ici plus tard une fois créé dans App Store Connect.

  android-workflow:
    name: Android Workflow
    max_build_duration: 60
    environment:
      flutter: stable
      vars:
        PACKAGE_NAME: "com.onnoff.ambutime"
    scripts:
      - name: Get Flutter packages
        script: flutter pub get
      - name: Build APK (debug - test pipeline)
        script: |
          flutter build apk --debug
    artifacts:
      - build/**/outputs/**/*.apk
      - build/**/outputs/**/mapping.txt
